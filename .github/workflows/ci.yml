name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  actions: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11']

    services:
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
          poetry install

      - name: Lint with ruff
        run: |
          poetry run ruff check src tests

      - name: Format check with black
        run: |
          poetry run black --check src tests

      - name: Type check with mypy
        run: |
          poetry run mypy src

      - name: Run tests with coverage
        run: |
          poetry run pytest tests --tb=short -m "not integration and not simulation" --cov=src/codetoreum --cov-report=xml
        env:
          REDIS_URL: redis://localhost:6379
          # Token scoped by workflow-level permissions (contents:read, actions:read)
          # 'actions' scope required for tests that query CI status via GitHub API / gh CLI
          GITHUB_TOKEN: ${{ github.token }}

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
          fail_ci_if_error: false
