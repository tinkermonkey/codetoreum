# Repair Cycle Test Scenario — Orchestrator: Project Configuration
#
# Port/Interface: IConfigStore
# Adapter (sim): InMemoryConfigStore

project_configs:
  - id: "repair_cycle_test_project"
    name: "repair_cycle_test_project"
    github_org: "demo-org"
    github_repo: "repair-cycle-test"
    tech_stacks:
      python: "3.11"
    testing:
      command: "pytest"
      framework: "pytest"
    environment_variables: {}
