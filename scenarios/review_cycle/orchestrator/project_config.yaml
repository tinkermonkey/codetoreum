# Review Cycle Scenario — Orchestrator: Project Configuration
#
# Port/Interface: IConfigStore
# Adapter (sim): InMemoryConfigStore

project_configs:
  - id: "review-cycle-project"
    name: "review-cycle-project"
    github_org: "demo-org"
    github_repo: "review-cycle-project"
    tech_stacks:
      python: "3.11"
    testing:
      command: "pytest"
      framework: "pytest"
    environment_variables: {}
