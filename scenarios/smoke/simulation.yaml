# Smoke Scenario — Simulation Settings
#
# Port/Interface: SimulationConfig (speed, clock, behaviour)
# Adapter: SimulationClock, SimulationApplicationBootstrap

name: "Smoke Scenario"
description: "Basic test scenario with minimal configuration for smoke testing"
version: "1.0"

speed_multiplier: 100.0
auto_advance: false

metadata:
  scenario_type: "smoke_test"
  author: "codetoreum-team"
  tags:
    - "basic"
    - "smoke-test"
