# Failure Recovery Scenario — Orchestrator: Project Configuration
#
# Port/Interface: IConfigStore
# Adapter (sim): InMemoryConfigStore

project_configs:
  - id: "failure-recovery-project"
    name: "failure-recovery-project"
    github_org: "demo-org"
    github_repo: "failure-recovery-project"
    tech_stacks:
      python: "3.11"
    testing:
      command: "pytest"
      framework: "pytest"
    environment_variables: {}
