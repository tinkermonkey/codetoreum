"""End-to-end tests for Codetoreum.

These tests require production infrastructure (real GitHub repos, Docker, etc.)
and should only be run with explicit environment setup.

Tests marked with @pytest.mark.e2e are skipped by default in CI.
"""
